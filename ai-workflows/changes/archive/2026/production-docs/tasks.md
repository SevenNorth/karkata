# 实施任务：完善官网生产接入文档

## 任务

- [x] 1. Red：在页面 manifest 注册五组中英文生产路由，确认站点契约因页面缺失而失败。
- [x] 2. Green：新增生产架构与安全页面，准确说明三种拓扑、凭据、授权、受限网关和数据边界。
- [x] 3. Green：新增配置、错误和部署页面，覆盖当前公共契约与可执行上线检查。
- [x] 4. Refactor：更新中英文导航和相邻页面交叉链接，修正旧 scope 文本并检查双语结构一致性。
- [x] 5. 运行文档契约、示例类型检查和静态构建，核对 18 组双语路由。
- [x] 6. 运行 `npm run check` 与 diff/工作区检查，记录结果并完成 change。

## TDD 记录

| 行为 | Red 命令与失败摘要 | Green 命令与结果 | Refactor 后结果 |
| --- | --- | --- | --- |
| 生产页面配对与路由 | manifest 增加路由后契约预期失败 | 18 组中英文页面配对、frontmatter、链接通过 | 中英文标题、路径和交叉链接复核通过 |
| 配置与错误契约准确性 | 人工核对 Core 类型、Provider 配置和错误码 | 配置默认值、错误分类、取消和重试策略写入页面 | 未修改 Runtime 或公共 API |
| 静态站输出 | 新页面缺失时未生成完整站点 | `npm run docs:build` 通过，生成 36 条路由 | 静态输出契约通过 |

## 验证记录

| 检查 | 结果 | 备注 |
| --- | --- | --- |
| `npm run test:docs` | 通过 | 12 项文档测试通过，页面配对有效：18 |
| `npm run docs:build` | 通过 | VitePress 构建和静态输出通过：36 routes |
| `npm run check` | 通过 | 182 项测试、全仓 typecheck 和四包构建通过 |
| `git diff --check` | 通过 | 无空白错误 |

## 实施备注

不实现后端；可部署参考应用保留为独立后续 change。当前工作区改动未创建 Git commit，等待用户审阅和后续授权。
